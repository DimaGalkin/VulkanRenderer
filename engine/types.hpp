#pragma once

namespace tdl {
    enum class Unit {
        Radians,
        Degrees
    };

    enum class Control {
        Controlled,
        Independent
    };

    enum class File {
        Video,
        Image
    };
};